plugins { id 'com.android.application' }
android {
 namespace 'cl.smarthub.porton'
 compileSdk 35
 defaultConfig {
  applicationId 'cl.smarthub.porton'
  minSdk 26
  targetSdk 35
  versionCode 4
  versionName '0.2.2-google'
  ndk { abiFilters 'arm64-v8a', 'armeabi-v7a' }
 }
 testOptions { unitTests.all { testLogging { exceptionFormat 'full' } } }
 compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
}
dependencies {
 // Credential provider excludes loader transitively; FragmentActivity still references it at runtime.
 implementation 'androidx.loader:loader:1.1.0'
 implementation 'androidx.browser:browser:1.8.0'
 implementation 'androidx.webkit:webkit:1.12.1'
 implementation 'androidx.credentials:credentials:1.3.0'
 implementation 'androidx.credentials:credentials-play-services-auth:1.3.0'
 implementation 'com.google.android.libraries.identity.googleid:googleid:1.1.1'
 implementation 'net.java.dev.jna:jna:5.18.1@aar'
 implementation 'com.alphacephei:vosk-android:0.3.75@aar'
 testImplementation 'junit:junit:4.13.2'
}
